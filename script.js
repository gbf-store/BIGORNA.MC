const products=[{name:"VIP Prata",mat:"Ferro",price:7,img:"prata.svg",desc:"Entrada premium com benefícios essenciais."},{name:"VIP Ouro",mat:"Ouro",price:10,img:"ouro.svg",desc:"Mais vantagens e destaque no servidor."},{name:"VIP Esmeralda",mat:"Esmeralda",price:13,img:"esmeralda.svg",desc:"Benefícios avançados para sua jornada."},{name:"VIP Diamante",mat:"Diamante",price:16,img:"diamante.svg",desc:"Pacote premium com benefícios superiores."},{name:"VIP Celestial",mat:"Netherite",price:20,img:"celestial.svg",desc:"O nível máximo da loja Bigorna MC."}];let cart=[];const $=x=>document.querySelector(x),money=x=>x.toLocaleString("pt-BR",{style:"currency",currency:"BRL"});function toast(x){let e=$("#toast");e.textContent=x;e.classList.add("show");setTimeout(()=>e.classList.remove("show"),2200)}function productsUI(){$("#products").innerHTML=products.map((p,i)=>`<article class="card"><div class="pic" style="background-image:url("assets/${p.img}")"></div><div class="body"><small>${p.mat}</small><h3>${p.name}</h3><p>${p.desc}</p><div class="price">${money(p.price)}</div><div class="actions"><button class="buy" data-buy="${i}">COMPRAR AGORA</button><button class="add" data-add="${i}">ADICIONAR AO CARRINHO</button></div></div></article>`).join("")}function add(i){let p=products[i],x=cart.find(a=>a.name==p.name);x?x.q++:cart.push({...p,q:1});render();toast(p.name+" adicionado ao carrinho")}function render(){let n=cart.reduce((s,x)=>s+x.q,0);$("#count").textContent=n;$("#items").innerHTML=cart.length?cart.map((p,i)=>`<div class="item"><div><strong>${p.name}</strong><small>${p.mat} • ${money(p.price)} cada</small></div><div class="qty"><button data-dec="${i}">−</button> ${p.q} <button data-inc="${i}">+</button></div><button class="remove" data-rem="${i}">REMOVER</button></div>`).join(""):`<div class="empty">Seu carrinho está vazio.</div>`;$("#total").textContent=money(cart.reduce((s,x)=>s+x.price*x.q,0))}function open(){render();$("#modal").hidden=false}function close(){$("#modal").hidden=true}document.addEventListener("click",e=>{let t=e.target;if(t.dataset.add!==undefined)add(+t.dataset.add);if(t.dataset.buy!==undefined){add(+t.dataset.buy);open()}if(t.dataset.inc!==undefined){cart[+t.dataset.inc].q++;render()}if(t.dataset.dec!==undefined){let i=+t.dataset.dec;cart[i].q--;if(cart[i].q<1)cart.splice(i,1);render()}if(t.dataset.rem!==undefined){cart.splice(+t.dataset.rem,1);render()}});$("#cartBtn").onclick=open;$("#heroCart").onclick=open;$("#close").onclick=close;$("#modal").onclick=e=>{if(e.target.id=="modal")close()};$("#checkout").onclick=()=>cart.length?toast("Pagamento iniciado. Envie o comprovante no ticket do Discord."):toast("Adicione um VIP ao carrinho primeiro.");for(let i=0;i<40;i++){let p=document.createElement("i");p.className="particle";p.style.left=Math.random()*100+"%";p.style.bottom="-5px";p.style.animationDuration=7+Math.random()*14+"s";p.style.animationDelay=-Math.random()*18+"s";$("#particles").appendChild(p)}productsUI();render();